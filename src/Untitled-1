
import React from 'react';
import { View, Text, StyleSheet, Image } from 'react-native';



export default function HomeScreen({ navigation }) {

    return (
        <View style={styles.container}>
            <View style={styles.settion_Header}>

                <Image
                    style={styles.image}
                    source={require('../image/header.png')} />

                <View style={styles.settion_body}>

                </View>
            </View>
        </View>

    )
}

const styles = StyleSheet.create({
    container: {
        flex: 1, flexDirection: 'column'
    },
    settion_Header: {
        flexDirection: 'column', flex: 1, backgroundColor: 'red', 

    },
    settion_body: {
        flexDirection: 'column', flex: 1 ,backgroundColor: 'blue', borderTopStartRadius: 20, borderTopEndRadius: 20,  
      
    },
    image: {
        flex: 1,
        width: '100%',
        height: '100%',
        resizeMode: 'cover',
       
       

    }
})